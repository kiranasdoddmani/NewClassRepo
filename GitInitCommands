# Init commands(used  for creating a new repo(folder)):---

# mkdir NewClassRepo
# cd NewClassRepo
# git init 

# after git init we go to github and create new repo with same name ie(NewClassRepo) and no need to add README.md we add README.md through vs code and using push command we push README.d file to github

# Branch Commonds

# git branch(used for see the branches)
# git branch -m BranchNewName (used for new Branch name)
# git checkout <- branch name-> (to navigate)from one branch to anthor branch jump
# git checkout -b <-NewBranchName->(for new branch name)
# git branch -d<-branchName->(to delete branch)
